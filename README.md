# Multi_Type_HERs

This project includes the atomic structures and detailed data of the training and test sets, feature extraction scripts, the process of building machine learning and deep learning models, and the validation results from the validation set.
